# Deliverable 03: Document-to-Code Traceability Matrix
**Project:** ForgeStudio SaaS Platform  
**Date:** September 2026  
**Scope:** Complete Cross-Reference of Provided Specifications against Existing Code  

---

## 1. Traceability Matrix Table

| Document Requirement | Existing Implementation | Relevant Files | API/Database Dependency | Current Status | Missing Work | Risk | Recommended Approach |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **RBAC: 7 Global Roles**<br>(Super Admin, Platform Admin, Support Admin, Developer, AI/Content Admin, Customer/Owner, Team Member) | `UserRole` enum only contains `USER`, `ADMIN`, `SUPER_ADMIN`. | `schema.prisma:212`<br>`api-v1.auth.ts`<br>`AuthContext.tsx` | PostgreSQL `User.role` enum | **PARTIALLY IMPLEMENTED** | Add missing roles to schema; update JWT payload; support platform staff views. | Role escalation or insufficient staff isolation. | Expand Prisma enum additively with backward-compatible migration. Add tenant-safe middleware. |
| **RBAC: 7 Project Roles**<br>(Owner, Admin, Designer, Developer, Content Editor, SEO Manager, Viewer) | `TeamMember.role` and `WebsiteCollaborator.permission` support `OWNER`, `ADMIN`, `DESIGNER`, `CONTENT_EDITOR`, `REVIEWER`. | `permission.service.ts:12`<br>`schema.prisma:635` | PostgreSQL `team_members`<br>`website_collaborators` | **PARTIALLY IMPLEMENTED** | Add explicit `DEVELOPER`, `SEO_MANAGER`, `VIEWER` roles and mapping definitions. | Inconsistent role semantics across teams and websites. | Unify project roles in `permission.service.ts` and ensure UI role selectors present standard roles. |
| **RBAC: Granular Capability Middleware**<br>(User -> Global Role -> Project Role -> Permission -> Action -> Allow/Deny) | Implemented in `canUserAccessResource()` with specific and wildcard overrides. | `permission.service.ts:75`<br>`website.routes.ts:62` | PostgreSQL `granular_permissions` table | **FULLY IMPLEMENTED** | Complete fine-grained action tagging across all sub-resource routes. | Low. | Keep existing architecture; extend capability constants additively. |
| **Audit Logging System**<br>(Log auth, permissions, WP connect, publish, delete, billing, suspension) | `AuditLog` table and `recordAuditLog()` service exist and are called by permissions, teams, and orgs. | `schema.prisma:343`<br>`audit.service.ts`<br>`auditLog.routes.ts` | PostgreSQL `audit_logs` table | **PARTIALLY IMPLEMENTED** | Add audit capture calls to website deletion, billing plan changes, and login/logout events. | Security compliance gaps during enterprise audit. | Add centralized audit dispatcher to auth and billing controllers. |
| **WordPress Plugin API: Auth Connect / Verify / Disconnect** | Backend routes exist for connect, status, verify, disconnect. | `wordpress.controller.ts`<br>`connector.service.ts`<br>`website.routes.ts:67` | PostgreSQL `wordpress_connections` table | **PARTIALLY IMPLEMENTED** | PHP connector plugin is missing from the repository; endpoint uses hardcoded simulated response. | Client thinks site is synced when no WP instance was contacted. | Implement real WordPress connector plugin in `/wordpress-plugin` and dispatch real HTTPS requests. |
| **WordPress Plugin API: 26 MVP Endpoints**<br>(`/pages`, `/publish`, `/media`, `/forms`, `/webhooks`, `/seo`) | Backend has partial endpoints; WordPress connector endpoints are stubs. | `wordpress/connector.service.ts`<br>`wordpress/transformer.service.ts` | Remote WordPress REST API | **PARTIALLY IMPLEMENTED** | Complete WordPress plugin endpoints; connect backend publisher to real WP REST endpoints. | High: publishing to real WP sites fails. | Build `forgestudio-connector` PHP plugin compliant with the WP Plugin API specification. |
| **Publishing State Machine**<br>(DRAFT -> VALIDATING -> BUILDING -> UPLOADING -> PUBLISHING -> PUBLISHED / FAILED) | Deployment status tracks `QUEUED`, `VALIDATING`, `BUILDING`, `PROCESSING`, `DEPLOYING`, `VERIFYING`, `PUBLISHED`, `FAILED`. | `schema.prisma:534`<br>`publishing.service.ts:304` | PostgreSQL `deployments` table | **FULLY IMPLEMENTED** | Reconcile async job transitions and UI polling/SSE hooks. | Low. | Retain existing state machine; ensure UI modal visualizes progress states in real time. |
| **Background Job Queue & Workers**<br>(Redis + BullMQ or DB Queue for async publish, verify, webhooks) | PostgreSQL-backed `BackgroundJob` table with in-process worker interval. | `schema.prisma:780`<br>`jobRunner.ts`<br>`scheduleWorker.ts` | PostgreSQL `background_jobs` table | **FULLY IMPLEMENTED** | Optional Redis adapter for high-scale multi-worker clusters. | In-memory fallback may lose jobs on server crash if DB write fails. | Retain PostgreSQL durable queue as reliable default; ensure worker idempotency. |
| **Visual Canvas & Drag-and-Drop Editor**<br>(Containers, sections, columns, widgets, navigator, history) | Implemented in `WebsiteEditor.tsx` (~17k lines) with HTML5 DnD, undo/redo, layers navigator, and inspector. | `WebsiteEditor.tsx`<br>`renderers.tsx`<br>`types/index.ts` | Local React State + Canonical JSON | **FULLY IMPLEMENTED** | None on core drag-and-drop; needs component modularization for maintainability. | High cognitive complexity due to file size. | Preserve working code; do not rewrite; extract non-critical sub-inspectors additively. |
| **CanonicalWebsiteData Schema**<br>(Multi-page, site settings, global styles, site parts, navigation) | Defined in `types/index.ts`, stored in PostgreSQL `websites.editorData`, validated in `publishing.service.ts`. | `types/index.ts:1306`<br>`publishing.service.ts:317` | PostgreSQL `websites.editorData` JSONB | **FULLY IMPLEMENTED** | Complete dynamic data binding tags within canonical element schema. | Low. | Keep CanonicalWebsiteData as single source of truth across all renderers. |
| **Static Compiler & ZIP Export**<br>(Compile canonical data to static HTML5/CSS3/JS bundle, download ZIP) | Implemented in `staticCompiler.ts` and `staticZip.service.ts`. | `staticCompiler.ts`<br>`staticZip.service.ts` | `archiver` library, Filesystem | **PARTIALLY IMPLEMENTED** | Expand static HTML renderer to cover all 40+ Pro and WooCommerce widgets beyond core set. | Missing widgets in exported HTML sites. | Add widget-to-static-HTML mappings in `staticCompiler.ts` matching `renderers.tsx`. |
| **SFTP Transport Publisher**<br>(Real SSH/SFTP connection, recursive directory creation, byte transfer) | Implemented with `ssh2-sftp-client` in `sftp.publisher.ts`. | `sftp.publisher.ts`<br>`sftp.service.ts` | `ssh2-sftp-client`, PostgreSQL | **FULLY IMPLEMENTED** | Add test connection UI feedback in settings modal. | Low. | Architecture is solid, tested, and fully functional. |
| **Theme Builder: Headers & Footers**<br>(Global templates, conditional display rules, sticky controls) | Implemented via `siteParts` in `CanonicalWebsiteData` and `ThemeLocationRule` model in DB. | `schema.prisma:327`<br>`PublishedSite.tsx:165`<br>`staticCompiler.ts:165` | PostgreSQL `theme_location_rules` | **FULLY IMPLEMENTED** | Add UI rule builder in frontend for archive/single conditions. | Medium. | Extend existing ThemeLocationRule APIs to frontend site settings. |
| **Custom Post Types (CPT) & Custom Fields**<br>(Post types, fields, entries, REST exposure) | Implemented in backend `customPostType.routes.ts` and frontend `CustomPostTypesPanel.tsx`. | `customPostType.controller.ts`<br>`CustomPostTypesPanel.tsx` | PostgreSQL `custom_post_types`, `custom_fields`, `custom_entries` | **FULLY IMPLEMENTED** | Connect CPT entries directly to visual editor Loop Grid widget. | Medium. | Build dynamic query resolver connecting CPT entries to editor element data feeds. |
| **Form Builder & Lead Management**<br>(Form fields, submission handling, spam protection, export) | Implemented in `form.service.ts`, `FormSubmission`, and `FormWidgetRenderer`. | `form.service.ts`<br>`FormWidgetRenderer.tsx` | PostgreSQL `form_submissions` table | **PARTIALLY IMPLEMENTED** | Add lead export to CSV and webhook notification dispatch on submission. | Low. | Add CSV export endpoint in `form.controller.ts` and trigger `WEBHOOK_RETRY` job. |
| **Popups & Triggers**<br>(Exit intent, scroll, timer, click, frequency capping) | Implemented in `PopupManagerModal.tsx`, `PopupRuntimePreview.tsx`, and `PopupSettingsPanel.tsx`. | `editor/components/Popup*.tsx`<br>`PublishedSite.tsx` | `Website.editorData.popups` | **FULLY IMPLEMENTED** | Test exit intent on mobile/touch devices. | Low. | Preserve existing implementation; ensure runtime handles popup DOM isolation. |
| **Dynamic Fonts System**<br>(Google Fonts loader, system fonts, dynamic font stylesheets) | Implemented in `features/fonts/` and `FontPickerModal.tsx`. | `FontLoader.ts`<br>`FontRegistry.ts`<br>`FontPickerModal.tsx` | Browser DOM, Google Fonts API | **FULLY IMPLEMENTED** | Support local custom font file uploads (`.woff2`). | Low. | Add file upload handler in `upload.controller.ts` for custom font files. |
| **Atomic Editor & Design System**<br>(Variables, Classes, Global Elements, Reusable Components) | Implemented in `features/atomic-editor/` with complete hooks, services, and UI panels. | `features/atomic-editor/*`<br>`WebsiteEditor.tsx` | `Website.editorData` | **FULLY IMPLEMENTED** | Synchronize CSS variable definitions with static compiler stylesheet generator. | Low. | Keep existing atomic editor code; export CSS variables in `generateGlobalCss()`. |
| **Subscriptions & Entitlements**<br>(Free vs Pro, site limits, storage limits, feature gating) | Implemented in `subscription.service.ts` and `subscription.middleware.ts` with `SubscriptionPlan`. | `subscription.service.ts`<br>`SubscriptionPage.tsx` | PostgreSQL `subscription_plans`, `user_subscriptions` | **PARTIALLY IMPLEMENTED** | Real Stripe payment intent creation and webhook fulfillment handler are stubs. | Revenue leakage or manual entitlement provisioning required. | Connect real Stripe API checkout sessions and webhook processing in backend. |
| **Admin & Super Admin Dashboards**<br>(Platform analytics, user management, site monitoring, system health) | `AdminDashboard.tsx` and `SuperAdminDashboard.tsx` exist but contain placeholder text and logout button. | `AdminDashboard.tsx`<br>`SuperAdminDashboard.tsx` | PostgreSQL `User`, `Website`, `AuditLog` | **PARTIALLY IMPLEMENTED** | Build real management UI: user list, website suspension, audit log explorer, plan management. | Administrators cannot govern the platform from UI. | Connect existing backend services (`operations.routes.ts`, `auditLog.routes.ts`, `users`) to rich UI. |
